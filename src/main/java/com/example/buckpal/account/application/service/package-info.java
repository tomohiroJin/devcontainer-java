/**
 * ユースケースの実装 (サービス) を置く。入力ポートを実装し、出力ポートを利用する。
 *
 * <p>依存ルール: adapter 層には依存しないこと (依存は port インターフェースまで)。 例: {@code SendMoneyService implements
 * SendMoneyUseCase}.
 */
package com.example.buckpal.account.application.service;
